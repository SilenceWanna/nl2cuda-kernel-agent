# 工作计划：自然语言 → CUDA 前反向 Kernel 自动生成 Skill

> 本文件是项目的**执行清单**。每完成一个任务，把 `[ ]` 改为 `[x]` 并在该行末尾追加 `(完成于 YYYY-MM-DD)`，然后 commit + push。
> 任务契约与验收标准见 [工作目标.md](工作目标.md)。

## 交付形态（2026-07-06 澄清）

**不构建新 agent**。交付物 = 一个 **agent skill**（`SKILL.md` 方法论指令 + `scripts/` 独立 python 工具），预留 **loop** 驱动多轮"生成→编译→验证→profile→改进"迭代。挂到现成 agent 上即可用；**先以 Claude Code 作宿主跑通/验收**，skill 写成通用格式（不绑定专有 API）便于移植到 Codex/开源 agent。最终宿主选用先不定。

## 环境基线（已确认）

- 开发机：Windows 11，无 NVIDIA GPU，仅写代码 + git。
- GPU 环境：Google Colab（免费版 Tesla T4，sm_75，nvcc 12.8，PyTorch 2.11.0+cu128，numpy 2.0.2）。GPU 配置非当前重点，后续如需可升级。
- 协作方式：代码放 GitHub **public** 仓库（https://github.com/SilenceWanna/nl2cuda-kernel-agent），Colab 里 clone/pull 运行（无需认证），输出回贴。
- 验收用例（已定）：**成对距离 / RBF 高斯核矩阵**（非 attention），FP32，形状固定 **N=M=4096, D=64**。
  - 结构：X:[N,D], Y:[M,D] → 距离 `Dist[i,j]=||x_i-y_j||²`，RBF 核 `K[i,j]=exp(-gamma·Dist[i,j])`；输出 K:[N,M]；反向求 dX、dY。
  - **赢面关键前提**：PyTorch 参考实现必须写成**自然广播形式** `(x[:,None]-y[None]).square().sum(-1)`（torch.compile 生成通用 Triton，缺 GEMM 式 D 倍数据复用，可被手写 tiling kernel 打败）；**不能**写成 `||x||²+||y||²-2·XYᵀ` 分解（会走 cuBLAS GEMM，难赢）。用广播形式是正当的（自然语言最直接翻译）。
- T4 约束：不支持 TF32；FP32 路径优化靠 tiling / float4 / warp 原语 / 算子融合。

---

## 阶段 0 — 验收基础设施（GPU 无关，可先写；在 Colab 验证）

> 这套设施双重身份：既是**判定 skill 是否达标的评测器**，也是 **skill 交给宿主 agent 的只读工具**。按防作弊要求，计时/验证脚本对 agent 只读、进程隔离。

- [x] **0.1** 项目骨架：目录结构（`skill/` `reference/` `tests/` `benchmarks/` `kernels/` `notebooks/` `scripts/`）、`README.md`、`requirements.txt`。(完成于 2026-07-07)
- [ ] **0.2** 敲定验收用例精确规格：`reference/config.py`，固定 RBF 核的 N=M=4096、D=64、gamma、dtype=fp32、随机种子列表（≥5）。
- [ ] **0.3** 手写 PyTorch 参考实现 `reference/rbf_reference.py`：RBF 核前向 + autograd 反向，作为正确性金标准。**用自然广播形式** `(x[:,None]-y[None]).square().sum(-1)` 表达，不落回 cdist/GEMM 分解。
- [ ] **0.4** 正确性验证器 `tests/verify.py`：传入一个 kernel 实现即可复用；≥5 组随机种子跑前向 allclose(atol=rtol=1e-2) + 反向各梯度 allclose；小规模 double `gradcheck`。任一不过即判失败。
- [ ] **0.5** 计时基准器 `benchmarks/bench.py`：warmup≥10、正式≥100 取几何均值、CUDA events、每次前后 synchronize、3 次重跑算 CV、CV>5% 作废重测；前反向分别计时；对比 `torch.compile`(默认 mode)。**设计为只读、进程隔离、不被 agent 改动**。
- [ ] **0.6** Colab 驱动 notebook `notebooks/run.ipynb`：git clone/pull + 装依赖 + 运行探测/验证/基准入口。先跑通"环境探测"确认回环可用。
- [ ] **0.7** ⚠️**赢面前置确认**：在 T4 实测 torch.compile(广播形式参考) 的前反向 baseline 数字，并粗估手写 tiling kernel 的理论加速比，确认"≥5% 可达"再进入阶段 1。若发现 torch.compile 意外走了 GEMM/难以打败，及时回到目标选择讨论。

## 阶段 1 — 手写参考 kernel 打通管线（自检 + 作为 skill 的参考解/调试样本，非最终交付）

> 目的：证明"编译→正确→计时→对比 torch.compile"这条评测管线本身可用，并摸清 T4 上打赢 torch.compile 的难度；产物同时作为 skill 提示里的正确解示例。

- [ ] **1.1** CUDA 扩展加载脚手架 `kernels/loader.py`：`torch.utils.cpp_extension.load` 即时编译，参数针对 sm_75。
- [ ] **1.2** 手写 RBF 核 **前向** kernel（朴素正确版）`kernels/rbf_forward.cu`。
- [ ] **1.3** 前向接入验证器：allclose 对拍参考实现通过（≥5 组种子）。
- [ ] **1.4** 手写 RBF 核 **反向** kernel（朴素正确版）`kernels/rbf_backward.cu`：计算 dX、dY。
- [ ] **1.5** 反向接入验证器：各梯度 allclose + 小规模 gradcheck 通过。
- [ ] **1.6** 首次基准：手写 kernel vs torch.compile 前反向分别计时，记录 baseline（用于摸差距、指导阶段 3 优化方向）。

## 阶段 2 — 编写 Skill（方法论指令 + 工具，宿主无关）

- [ ] **2.1** Skill 设计文档 `skill/DESIGN.md`：skill 的输入/输出、agent 每步该做什么、可用工具（验证器/基准器/加载器）、失败反馈如何回喂、防作弊红线。
- [ ] **2.2** 编写 `skill/SKILL.md`（方法论主体）：指导宿主 agent 从 NL 描述 → PyTorch 参考 → CUDA 前反向 → 用验证器自检 → 用基准器测速的完整步骤与判据（自然语言，宿主无关）。
- [ ] **2.3** Skill 工具封装 `skill/scripts/`：把验证器、基准器、编译加载封装成命令行工具（agent 一条命令可调用，只读评测脚本），带清晰 stdout 反馈供 agent 读取。
- [ ] **2.4** loop 驱动脚本 `skill/loop.md`（或 py）：定义"生成→编译→验证→读 profile→改进"的迭代循环与终止条件（正确且前反向均达标 / 到达轮次上限）。
- [ ] **2.5** Skill 干跑测试：喂 RBF 核的 NL 描述，宿主 agent 跑到"正确性通过的前反向 kernel"（先不要求达速度标）。

## 阶段 3 — 优化冲刺（由 skill/loop 驱动宿主 agent，打赢 torch.compile ≥5%）

- [ ] **3.1** profile 反馈接入 skill：让 agent 能读 profiler 输出（访存/占用率/bank conflict）并据此改 kernel。
- [ ] **3.2** 在 skill 指导下迭代**前向**优化：tiling + float4 + warp 原语 + 融合，逼近并超过 torch.compile 前向。
- [ ] **3.3** 在 skill 指导下迭代**反向**优化：同类手段超过 torch.compile 反向。
- [ ] **3.4** 达标确认：规范计时下前反向均 ≥1.05×，3 次重跑 CV≤5%，正确性仍通过。（如 T4 难达标，评估升级 GPU。）

## 阶段 4 — 端到端验收与交付

- [ ] **4.1** Skill 端到端验收：以 Claude Code 为宿主，仅凭 NL 描述 + skill，自动产出通过正确性且前反向均 ≥1.05× 的 `.cu`。
- [ ] **4.2** 交付自检：`.cu` 独立编译通过 + 防作弊清单逐条核对（无 SDPA/F.* 落回、未改计时脚本、未降精度、无 torch 高层运行时依赖）。
- [ ] **4.3** Skill 通用性说明 + README：记录如何挂到其他宿主（Codex/开源 agent），收尾。

---

## 进度日志

- 2026-07-06：仓库初始化、环境确认（Colab T4）、工作计划成文。
- 2026-07-06：按用户澄清修订计划——交付物改为 **skill（+预留 loop）**，先以 Claude Code 跑通；阶段 2 由"构建 agent"改为"编写 skill"，阶段 1 手写 kernel 重定位为管线自检+参考解。
- 2026-07-07：**验收目标由 causal MHA 改为成对距离/RBF 核矩阵**（非 attention，避开 SDPA 专家 baseline 的不对称劣势）；形状定 N=M=4096, D=64, fp32；参考实现须用广播形式以保赢面；阶段 0 新增 **0.7 赢面前置确认**。
- 2026-07-07：完成 **0.1** 项目骨架（目录结构 + README + requirements.txt）。
